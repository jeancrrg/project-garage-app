import { SafeAreaView } from "react-native-safe-area-context";
import TopBar from "../../components/topbar/TopBar";

export default function MaisInformacoesScreen() {
    return (
        <SafeAreaView>
            <TopBar label='Mais Informações' />
        </SafeAreaView>
    );
}